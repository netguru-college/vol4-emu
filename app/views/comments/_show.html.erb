<% @event.comments.each do |comment| %>
  <tr>
    <td><%= image_tag comment.user.avatar_url :thumb, alt: 'Avatar' if current_user.avatar_url %></td>
    <td><%= comment.user.username %></td>
    <td><%= comment.body %></td>
  </tr>
<% end %>
